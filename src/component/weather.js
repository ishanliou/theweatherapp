import React, {Component } from 'react'

class Weather extends Component {
    render() {
        // const list = this.props.weatherList	
        // console.log(list)
        // const wList = Object.entries(list)
        // console.log(wList)
       
        // const newArr = wList.filter((_,i) => i % 4 == 0); 
        // console.log('result', newArr)
        // console.log('result', newArr[0])
        
        return(
            <div>weather
            

            </div>
        )
    }
}

export default Weather